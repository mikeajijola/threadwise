# Set a unique operator password locally; never commit real credentials.
COPILOT_PASSWORD=
# Required for private storage on Vercel; local development can use .data/.
BLOB_READ_WRITE_TOKEN=
# AI Gateway credential for local development, if required.
AI_GATEWAY_API_KEY=
# Optional local data location (keep outside Git if changed).
# DATA_DIRECTORY=.data
